{
  "name": "tour2playwright",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generate Playwright e2e tests and illustrated HTML user manuals from Tour Recorder JSON exports, written directly into the target module's own structure.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "gen": "node src/cli.mjs generate",
    "manual": "node src/cli.mjs manual",
    "build": "node src/cli.mjs build"
  }
}
